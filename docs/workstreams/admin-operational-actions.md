# Codex Cloud task: Controlled Admin Operations and Fulfilment (A3)

Repository: `Professor2080/poster-valley-kickoff-site`; base branch: merged A1 plus relevant A2 shell; branch `codex/admin-operational-actions`; draft PR **A3: Add controlled admin operations and fulfilment**. Goal: add only controlled custom-order operations, never a general commerce replacement.

Context/decisions: live custom flow owns invitations/orders/payments; Woo is separate; Auth is magic-link/roles with Pascal initial manager; server shipping remains authoritative; staging is mandatory. Customer-facing v1 emails: invitation, paid-order confirmation and shipping confirmation. `ready_to_pack`/`packed` are internal; manual international quotes need manager approval and explicit expiry. Preconditions: A1 merged with staging/RLS/contracts, A2 relevant shell available, QA review started. No agent changes frozen shared statuses.

Own `src/admin/**` action views, `api/admin/**` narrowly scoped action endpoints, action tests and A3 docs. Do not modify public routes, Mollie webhook/create-payment behavior, unrestricted `api/_mollie.js`, Woo docs/config, production environments or unrelated schema/contracts. Implement one invitation from one reservation; preview/send/resend idempotency and confirmation safeguards; manager-controlled manual quotes/expiry; fulfilment transitions/tracking/carrier; shipping confirmation; email/audit event history. No batch mail, no manual payment-to-paid change, no Woo writes.

Allowed: scoped code/migrations only if A1 contract explicitly calls for them, local/staging-safe tests and draft PR. Forbidden: Production Supabase/service key, production migration/deploy/merge, real emails/payments, secret exposure, hosting/Woo actions. Staging data must be removable and mail/payment suppressed. Run standard checks, diff/conflict scan, negative authorization/idempotency/transition/email tests and staging migration/RLS checks. Final report: files, actions/guards, event semantics, tests, staging evidence, risks and no-production confirmation. Done: audited, permissioned, confirmed actions with no payment-authority violation and A4 handoff.
